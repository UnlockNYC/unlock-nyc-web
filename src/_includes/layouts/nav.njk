<nav id="main">
  <div id="nav-wrapper">
    <div id="nav-logo">
      <a href="/">Unlock NYC</a>
      <!--<img id="hamburger" src="https://cdn.glitch.global/53a51cf9-3776-4b8a-a2d4-b91bd1be3da1/hamburger.svg?v=1691522037151">-->
      <svg id="hamburger" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
      <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
      </svg>
    </div>
    <!--
    {% set navPages = collections.all | eleventyNavigation %}
    <ul>
    {%- for entry in navPages %}
      <li{% if entry.url == page.url %} class="active-header"{% endif %}>
        <a href="{{ entry.url }}">{{ entry.title }}</a>
      </li>
    {%- endfor %}
      <li><a>Donate</a></li>
      <a><li class="login-button">Sign In</li></a>
    </ul>-->
    {% set navPages = collections.all | eleventyNavigation %}
    {% macro renderNavListItem(entry) -%}
    <li {% if entry.url == page.url %} class="active-header"{% endif %}>
      <a {% if entry.url == 'blank' %} class="dropdown" {% endif %} href="{{ entry.url }}">{{ entry.title }}</a>
    {%- if entry.children.length -%}
      <ul class="submenu">
        {%- for child in entry.children %}{{ renderNavListItem(child) }}{% endfor -%}
      </ul>
    {%- endif -%}
    </li>
    {%- endmacro %}

    <ul id="top">
    {%- for entry in navPages %}{{ renderNavListItem(entry) }}{%- endfor -%}
    <li><a>Donate</a></li>
    <li class="login-button">Report</li>
    <li class="login-button">Sign In</li>
    </ul>
  </div> <!-- end of nav wrapper-->
  
</nav>