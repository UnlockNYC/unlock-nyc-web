{% for thing in blocksList %}
<div class="blocks-wrapper{% if blocksFirst == 'image'%} image-first{% endif %}">
  <div class="block">
    <h4>
    {{ thing.title }}
    </h4>
    <p>
      {{ thing.text }}
    </p>
    {% if thing.button == 'yes' %}
    {% for button in thing.buttonList %}
      <button {% if blocksButtonColor == 'yellow'%} class="yellow" {% else %} class="black" {% endif %} onclick=window.location.href='{{ button.buttonLink }}';>
        {{ button.buttonText }}
      </button>
    {% endfor %}
    {% endif %}
  </div>
  <div class="block image">
    <img src="{{ thing.image }}" style="object-fit: cover; width: 100%;" alt="{{ thing.alt }}">
  </div>
</div>
{% endfor %}