<!DOCTYPE html>
<html lang="en">
  <head>
    {# This is the base template for the site – other templates extend this one #}
    <meta charset="utf-8" />
    <link rel="icon" href="https://glitch.com/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Unlock NYC Website 2.0 Draft" />
    <link rel="stylesheet" type="text/css" href="/public/style.css" />
    {% seo "" %}
    <!--scripts-->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script> 
  </head>
  <body>
    <main>
      {% include 'layouts/nav.njk' %} 
      
      {# Page or post content will appear here #}
      {{ content | safe }}

      {% include 'layouts/footer.njk' %} 

  </main>
    
  <script>
  /*MAB: MOVE INTO FILE! */
    $("#nav-logo").click(function() {
      if ($(window).width() <= 1100) {
        $("nav ul").toggleClass("mobile-open");
      }
    });
    $("nav#main ul#top li.main").on("mouseenter", function(){
      if ($(window).width() > 1100) {
        /*$("ul.submenu").hide();*/
        $(this).children("ul").show();
      }
    });
    $("ul.submenu").on("mouseleave", function() {
      $(this).hide();
    });
  </script>
   
  </body>
</html>